version: '3.8'

services:
  postgres:
    image: postgres:17
    container_name: ecom_database
    environment:
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: P@ssw0rd4dM!n
      POSTGRES_DB: ecom_transaction
    ports:
      - "5430:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    restart: unless-stopped

volumes:
  postgres_data: